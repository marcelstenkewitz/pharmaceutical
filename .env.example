# Pharmaceutical Tracking System - Environment Configuration
# Copy this file to .env and customize as needed

# ============================================
# BACKEND CONFIGURATION
# ============================================

# Environment mode (development, production, test)
NODE_ENV=production

# Backend server port
PORT=3001

# CORS allowed origins (comma-separated for multiple origins)
# For local deployment, use http://localhost:3000
# For production, set to your domain or * for all origins
CORS_ORIGIN=http://localhost:3000

# Data directory path (optional, defaults to /app/data in production)
# DATA_DIR=/custom/path/to/data

# Node.js memory limit (optional)
# NODE_OPTIONS=--max-old-space-size=2048

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# Backend API URL
# For local deployment: http://localhost:3001
# For production: your backend URL
REACT_APP_API_URL=http://localhost:3001

# ============================================
# DOCKER CONFIGURATION
# ============================================

# Compose project name (optional)
COMPOSE_PROJECT_NAME=pharmaceutical

# ============================================
# OPTIONAL SETTINGS
# ============================================

# Logging level (error, warn, info, debug)
# LOG_LEVEL=info

# Enable debug mode (true/false)
# DEBUG=false

# Session secret (if authentication is added)
# SESSION_SECRET=your-secret-key-here

# ============================================
# NOTES
# ============================================

# 1. This file is for reference only - copy to .env to use
# 2. Never commit .env files with actual secrets to version control
# 3. For local deployment, default values are usually sufficient
# 4. Update CORS_ORIGIN and REACT_APP_API_URL for production deployment
